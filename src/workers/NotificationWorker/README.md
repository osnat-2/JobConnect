# NotificationWorker

Responsibilities:
- Send email / push notifications for events (application received, status changed)
- Retry and backoff handling for delivery failures

Suggested tech: Node.js or Python with an email provider SDK

Run (example):
- Wire to message broker and secrets store for credentials
